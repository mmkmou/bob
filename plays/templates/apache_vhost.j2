# File generated by Ansible.

<VirtualHost *:80>

  DocumentRoot /var/www/{{ project }}/{{ hostname }}/{{ git_drupal_root_path }}

  ServerName {{ hostname }}
  ServerAlias www.{{  hostname }}

  ErrorLog "/var/log/apache2/{{ hostname }}-error.log"
  CustomLog ${APACHE_LOG_DIR}/{{ hostname }}-access.log combined

  <Directory /var/www/{{ hostname }}/{{ git_drupal_root_path }}>
    AllowOverride All
  </Directory>

</VirtualHost>
